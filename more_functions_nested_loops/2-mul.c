#include <stdio.h>
/**
 * mul - multiplies two integers
 * @a: is a param
 * @b: is a param
 * Return: result multiply a*b
 */

int mul(int a, int b)
{
	int result;

	result = (a * b);

	return (result);
}
